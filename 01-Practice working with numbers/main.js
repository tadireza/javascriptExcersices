/*I want to calculate the circumference of a circle. */
const radius = 5;
const circumference = 2* Math.PI* radius;
console.log(circumference);


/*I want to calculate the area of a circle. */
const r=6;
const area= Math.PI*Math.pow(r,2);
console.log(area);